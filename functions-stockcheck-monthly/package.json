{
  "name": "functions-stockcheck-monthly",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "node ./node_modules/typescript/bin/tsc",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.8.0",
    "firebase-functions": "^4.3.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
